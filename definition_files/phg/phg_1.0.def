Bootstrap: docker
From: maizegenetics/phg:1.0

%post
    cd /
    chmod +x *.sh
    chmod +x *.groovy

%environment
    export LC_ALL=C
    export LANG=en_US.UTF-8
    export LANGUAGE=en_US.UTF-8
    export PATH=/:$PATH

